# require 'rails_helper'

# RSpec.describe OpportunityNetwork, type: :model do
#   subject { OpportunityNetwork.new(network_id: 1, opportunity_id: 1) }

#   it { should validate_presence_of(:network_id) }
#   it { should validate_presence_of(:opportunity_id) }
#   it { should validate_uniqueness_of(:network_id)
#     .scoped_to(:opportunity_id) }

#   it { should belong_to(:opportunity) }
#   it { should belong_to(:network) }
# end
