json.extract! notification, :id, :recipient_id,
:actor_id, :action, :acted_with_type, :acted_with_id,
:targetable_type, :targetable_id, :read_at, :message,
:created_at

# json.extract! notification, :id, :message, :read_at
